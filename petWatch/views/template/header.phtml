<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo isset($page_title) ? $page_title : 'petWatch - Find Missing Pets'; ?></title>

    <!-- Bootstrap CSS files -->
    <link href="../css/bootstrap.css" rel="stylesheet">
    <link href="../css/bootstrap-theme.css" rel="stylesheet">

    <!-- My custom CSS file - HANDWRITTEN -->
    <link href="../css/my-style.css" rel="stylesheet">
</head>
<body>

<?php
// Start session if not already started
if (session_status() == PHP_SESSION_NONE) {
    session_start();
}
?>

<!-- Header section with navigation -->
<header>
    <nav class="navbar navbar-default">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#myNavbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="../index.php">
                    <img src="../images/pet-logo.png" alt="petWatch Logo">
                </a>
            </div>
            <div class="collapse navbar-collapse" id="myNavbar">
                <ul class="nav navbar-nav">
                    <li><a href="../index.php">Home</a></li>
                    <li><a href="../controllers/browse.php">Browse Pets</a></li>
                    <li><a href="../controllers/help.php">Help</a></li>

                    <!-- Show additional links based on user role and login status -->
                    <?php if (isset($_SESSION['loggedin']) && $_SESSION['loggedin'] === true): ?>
                        <?php if ($_SESSION['user_role'] === 'user'): ?>
                            <!-- Regular user (browse_users) sees these links -->
                            <li><a href="#">View sightings</a></li>
                            <li><a href="#">Report Sightings</a></li>
                        <?php elseif ($_SESSION['user_role'] === 'pet_owner'): ?>
                            <!-- Pet owner sees this link -->
                            <li><a href="#">Manage Pets</a></li>
                        <?php endif; ?>
                    <?php endif; ?>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <?php if (isset($_SESSION['loggedin']) && $_SESSION['loggedin'] === true): ?>
                        <!-- Show logout link if user is logged in -->
                        <li><a href="../controllers/logout.php" onclick="return confirmLogout()">Logout (<?php echo htmlspecialchars($_SESSION['username']); ?>)</a></li>
                    <?php else: ?>
                        <!-- Show login link if user is not logged in -->
                        <li><a href="../controllers/login.php">Login</a></li>
                    <?php endif; ?>
                </ul>
            </div>
        </div>
    </nav>
</header>

<!-- Main content container -->
<div class="container main-content">

    <!-- Simple JavaScript for logout confirmation -->
    <script>
        // Function to confirm logout
        function confirmLogout() {
            // Show confirmation dialog
            var confirmLogout = confirm("Are you sure you want to logout?");

            // If user clicks "Cancel", prevent the link from following through
            if (!confirmLogout) {
                return false;
            }

            // If user clicks "OK", the link will proceed normally
            return true;
        }
    </script>